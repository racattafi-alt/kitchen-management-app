[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm config set prefix /usr/local && npm install -g pnpm@10.4.1 && /usr/local/bin/pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["/usr/local/bin/pnpm build"]

[start]
cmd = "node dist/index.js"
