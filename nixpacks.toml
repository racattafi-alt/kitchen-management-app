[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npm install -g pnpm@10.4.1"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "node dist/migrate.js && node dist/index.js"
