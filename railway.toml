[build]
builder = "NIXPACKS"

[deploy]
# The server applies DB migrations automatically on startup via drizzle-orm/mysql2/migrator.
# DATABASE_URL must be set in Railway environment variables.
startCommand = "node dist/migrate.js && node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
